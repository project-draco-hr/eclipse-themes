{
switch (state) {
case STATE_DRAGGING:
    if (getSelctionArea().contains(event.x,event.y))     setState(STATE_HOVER);
 else     setState(STATE_NORMAL);
  break;
default :
setState(STATE_NORMAL);
break;
}
}
