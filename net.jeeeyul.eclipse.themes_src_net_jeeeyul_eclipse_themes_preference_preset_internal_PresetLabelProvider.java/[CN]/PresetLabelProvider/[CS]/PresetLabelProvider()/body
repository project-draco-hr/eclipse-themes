{
  imageRegistry=new ResourceRegistry<IJTPreset,Image>(new Function1<IJTPreset,Image>(){
    @Override public Image apply(    IJTPreset t){
      return t.getImageDescriptor().createImage();
    }
  }
);
}
