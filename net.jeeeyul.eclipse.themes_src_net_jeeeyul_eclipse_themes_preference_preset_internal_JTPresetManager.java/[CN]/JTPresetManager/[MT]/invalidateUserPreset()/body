{
  if (userPresets != null) {
    userPresets.clear();
    userPresets=null;
  }
  for (  IJTPresetManagerListener each : listeners) {
    try {
      each.userPresetModified();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
